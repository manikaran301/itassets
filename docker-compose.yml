version: '3.8'

services:
  mams-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mams-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
